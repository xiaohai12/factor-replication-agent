% Preamble: thesis presentation + notation macros used throughout the text.
% Keep this Overleaf-compatible (no local-only packages).

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{mathpazo}
\usepackage{microtype}
\usepackage{geometry}
\geometry{
  inner=2.5cm,
  outer=2.5cm,
  bindingoffset=0.5cm,
  top=2.5cm,
  bottom=2.5cm
}

\usepackage{amsmath, amssymb, amsthm}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{siunitx}
\usepackage{xcolor}
\usepackage{natbib}
\usepackage{scrlayer-scrpage}

% ---------------------------------------------------------------------------
% Page, heading, title-page, float, and link styling
% ---------------------------------------------------------------------------
\definecolor{ThesisBlue}{HTML}{17365D}
\definecolor{ThesisRule}{HTML}{7892AE}

\setkomafont{disposition}{\normalfont\bfseries\color{ThesisBlue}}
\setkomafont{chapter}{\normalfont\huge\bfseries\color{ThesisBlue}}
\setkomafont{section}{\normalfont\Large\bfseries\color{ThesisBlue}}
\setkomafont{subsection}{\normalfont\large\bfseries\color{ThesisBlue}}
\setkomafont{pageheadfoot}{\normalfont\small}
\setkomafont{pagenumber}{\normalfont\small}

\clearpairofpagestyles
\automark[section]{chapter}
\ihead{\headmark}
\ohead{\pagemark}
\KOMAoptions{headsepline=0.4pt}
\pagestyle{scrheadings}

\setlength{\parindent}{1.5em}
\setlength{\parskip}{0pt}
\setlength{\emergencystretch}{2em}
\setlength{\textfloatsep}{18pt plus 4pt minus 4pt}
\setlength{\floatsep}{14pt plus 3pt minus 3pt}
\setlength{\intextsep}{14pt plus 3pt minus 3pt}
\captionsetup{font=small,labelfont=bf,labelsep=period}

\newcommand{\HRule}{{\color{ThesisRule}\rule{\linewidth}{0.6pt}}}
\makeatletter
\renewcommand{\maketitle}{%
  \begin{titlepage}
    \thispagestyle{empty}
    \begin{center}
      \vspace*{0.08\textheight}
      {\scshape\Large Master's Thesis\par}
      \vspace{1.4cm}
      \HRule\\[0.55cm]
      {\huge\bfseries\color{ThesisBlue}\@title\par}
      \vspace{0.55cm}
      \HRule
      \vfill
      \begin{minipage}[t]{0.46\textwidth}
        \raggedright\large
        \emph{Author:}\\[0.2cm]
        \@author
      \end{minipage}%
      \hfill
      \begin{minipage}[t]{0.46\textwidth}
        \raggedleft\large
        \emph{Date:}\\[0.2cm]
        \@date
      \end{minipage}
      \vspace*{0.10\textheight}
    \end{center}
  \end{titlepage}%
}
\makeatother

\usepackage[
  colorlinks=true,
  linkcolor=ThesisBlue,
  citecolor=ThesisBlue,
  urlcolor=ThesisBlue,
  bookmarksopen=true
]{hyperref}
\hypersetup{
  pdftitle={Underdetermination in Factor Replication: A Controlled, Leakage-Proof LLM Agent as an Independent Second Implementer},
  pdfauthor={Your Name}
}

% ---------------------------------------------------------------------------
% Notation macros (see docs/paper-outline.md Ch.3 for the formal definitions)
% ---------------------------------------------------------------------------
% Paper's own reported number
\newcommand{\Pref}{P}
% Agent signal + agent-read paper configuration (paper-faithful baseline)
\newcommand{\Aref}{A}
% Agent signal + C&Z's actual configuration (config-swapped hybrid track)
\newcommand{\Acz}{A_{cz}}
% Agent signal + HXZ-style standardized configuration (config-swapped hybrid track)
\newcommand{\Ahxz}{A_{hxz}}
% C&Z's own measured/reported result (from SignalDoc.csv / CZReferenceProfile)
\newcommand{\CZ}{CZ}
% HXZ's own reported result for this factor (via hxz_bridge.compute_hxz_reported)
\newcommand{\HXZ}{HXZ}
% Agent-extracted config / C&Z's actual config / HXZ standardized config
\newcommand{\Cagent}{C_{\text{agent}}}
\newcommand{\Ccz}{C_{\text{cz}}}
\newcommand{\Cstd}{C_{\text{std}}}
% Agent signal series
\newcommand{\SA}{S_A}
% t-statistic shorthand
\newcommand{\tstat}{t}
% Implementation space admitted by a paper P
\newcommand{\ImplSpace}[1]{\mathcal{I}(#1)}

\theoremstyle{definition}
\newtheorem{definition}{Definition}
\newtheorem{assumption}{Assumption}
