\section{Validator and Leakage-Control Rules}
\label{app:validator}
% Step 4 sandbox validator rules (syntax/schema/future-leak scan); the
% leakage-proof boundary specifics (SignalDoc.csv never passed to the
% extractor; where/how the boundary is enforced in code).
