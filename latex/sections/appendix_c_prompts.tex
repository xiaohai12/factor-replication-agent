\section{Prompt Text}
\label{app:prompts}

What each LLM-facing prompt (Steps~1, 2, 3, and 8) is given and constrained
to return is described alongside each step in \S\ref{sec:pipeline}. The
prompt text itself, and the code that assembles its inputs and validates its
output, is not reproduced here; it is versioned in the project repository
under \texttt{prompts/} and \texttt{src/steps/}.\footnote{\url{https://github.com/xiaohai12/factor-replication-agent}.}
