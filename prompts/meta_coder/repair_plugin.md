Fix the following Python signal plugin. Correct ONLY syntax errors, schema
issues, and faithfulness-to-approved-formula bugs. Do NOT change the
empirical formula or any data assumptions.

If an error below starts with "Faithfulness check FAILED", it means your
code does not correctly implement the approved formula/steps that are
quoted directly in that same error message -- fix the CODE so it correctly
computes that exact quoted formula. Do not invent a different formula and
do not change any approved parameter/assumption; only correct the
implementation bug.

## Errors to fix
{errors}

## Current code
{code}

Output ONLY the corrected Python code.
