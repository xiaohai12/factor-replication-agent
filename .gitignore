# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
data/synthetic_data/

# Virtual environment
.venv/

# Data (large/downloaded artifacts are not committed)
data/papers/
data/CZ code/
data/local/

# All pipeline-run-generated artifacts (MethodSpecs, plugins, backtest scripts,
# evidence/RunRecords) live under runs/ — a single gitignored directory.
# Committed reference fixtures live in tests/fixtures/ instead (not ignored).
runs/

# Recommended FACTOR_AGENT_RUNS_DIR target for ad-hoc/manual test runs (see
# backend/state.py) -- keeps test-generated session/methodspec/evidence
# artifacts out of the real runs/ dir so they're easy to bulk-delete.
.runs_scratch/


# Curated research metadata is intentionally trackable:
# data/gold_standard/

# IDE
.idea/
.vscode/

# OS
.DS_Store

# pdf 
*.pdf

# CSV data stays local unless explicitly force-added.
*.csv
