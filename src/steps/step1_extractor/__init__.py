"""Step 1: extraction. The paper-first extractor lives in
`paper_extractor.py` (`PaperExtractor`, `build_paper_method_spec`,
`load_extraction_system_prompt`). The original v1 `SemanticExtractor`
(flat `MethodSpec` extraction) was retired along with `MethodSpec` itself.
"""
