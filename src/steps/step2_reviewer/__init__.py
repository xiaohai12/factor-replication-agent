"""Step 2: review + physical-field resolution. The paper-first
implementations live in `review.py` (`review_method_spec`)
and `implementation_resolution.py` (`build_implementation_resolution`).
The original v1 `ReviewGate`/`resolution.apply_decisions` (flat `MethodSpec`
rules-based review) were retired along with `MethodSpec` itself.
"""
